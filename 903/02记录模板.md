
# 流量模块



### 一、整体架构

* 这个模块在整个系统中的位置（上游是谁、下游是谁）
* 数据从哪来、经过什么处理、到哪去（画数据流图）
* 分几层 / 分几个线程 / 分几个文件
* 用了什么通信方式（DDS / 共享内存 / 网络）

### 二、我做了什么

* 具体改了哪些文件（文件路径记下来）、

	/work/sunboxue/LiuLiang/traffic_flow/src/traffic_flow/detectors
/work/sunboxue/LiuLiang/traffic_flow/src/traffic_flow/detectors/accumulate_headway_detector.cpp

* 新增了什么功能 / 改了什么逻辑

	1.将车头时距探测器加入到时间周期、信号灯周期中。新增accumulate_headway_detector.cpp/h文件。实现headway逻辑，然后在/work/sunboxue/LiuLiang/traffic_flow/src/traffic_flow/traffic_flow_factory.cpp中也引用。

	2.车头时距计算逻辑：headway 

		（1）车头时距：当一辆车的车头经过停止线的时候 记录下当时的时间戳 然后当下一辆车的车头经过停止线时 再记录此时的时间戳 二者做差就是车头时距。即代表前车通过到后车通过的时间差。

		（2）gap：第二辆车的车头时间戳 - 前车的车尾时间戳

		 （3）车尾时间戳：=车头时间戳＋车身长度/速度 

		（4）如何判断是否经过停车线：前后帧的车的位置 连成线 和停止线看看是否相交 相交则表示此帧过线了

* 改动的代码量大概多少行
* 花了多长时间完成

### 三、用的什么技术

* 语言版本（C++14 / C++17）
* 第三方库（nlohmann/json、FastDDS、NVML 等）
* 设计模式（工厂、单例、策略、观察者等）
* 并发机制（mutex、lock_guard、条件变量、无锁队列等）
* 数据结构（map、set、vector、queue 等，为什么选这个）

### 四、为什么这么设计

* 为什么用这个通信方式而不是别的
* 为什么数据要缓存 / 为什么要异步
* 为什么用这个数据结构
* 为什么接口这么定义（参数、返回值）
* 为什么分成这几层 / 这几个文件
* 有没有考虑过其他方案，为什么没选

### 五、遇到的问题

* 编译报错（命名空间冲突、include 路径、类型不匹配等）
* 运行报错（逻辑错误、空指针、数据不对等）
* 怎么定位的（加打印、gdb、日志等）
* 怎么解决的
* 花了多长时间

### 六、可以优化的点

* 性能瓶颈在哪（数据结构、算法复杂度、锁粒度）
* 代码质量问题（硬编码、重复代码、缺少错误处理）
* 如果时间充裕想怎么改
* 别人有没有提过改进方向

### 七、面试能用的一句话总结

* 用一句话说清楚"我在这个模块做了什么、用了什么技术、解决了什么问题"
* 例："我设计了基于 DDS 双数据流的信号灯接入模块，通过 mutex 保证跨线程状态安全，实现相位边界检测触发流量统计窗口重置"
