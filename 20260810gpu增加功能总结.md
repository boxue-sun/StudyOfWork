
# Jetson 边缘计算平台 GPU 监控功能开发总结

> 项目：AirOS 2.0 车路协同边缘计算平台 - MonitorService 监控服务
> 平台：NVIDIA Jetson (aarch64)
> 技术栈：C++、CUDA Runtime API、Linux 系统编程、Docker、CMake

---

## 一、任务背景（我做了什么）

### 1.1 项目背景

AirOS 是一个运行在 NVIDIA Jetson 边缘计算盒子上的车路协同系统，负责处理路侧摄像头、雷达等传感器数据。系统需要一个**监控服务（MonitorService）**实时采集设备状态（CPU/GPU/内存/磁盘/温度等），并以 JSON 格式上报给云端平台。

### 1.2 我的任务

1. **阅读 monitorservice3 的设备探测代码**（`jetson_probe.cpp`），理解现有的监控指标采集机制
2. **新增 GPU 监控功能**：
   - GPU 当前频率（MHz）
   - GPU 最大频率（MHz）
   - GPU 显存使用量（MB）
   - GPU 显存总量（MB）
   - GPU 显存占用率（%）
3. **在 Docker 容器中跑通整个监控 demo**

### 1.3 最终成果

```json
{
  "system.gpu_freq(MHz)": 306.0,
  "system.gpu_freq_max(MHz)": 930.75,
  "system.gpu_mem_total(MB)": 30588.01,
  "system.gpu_mem_usage(%)": 0.71,
  "system.gpu_mem_used(MB)": 218.36
}
```

GPU 显存数据从 CUDA API 的 ~6900MB（不准确）修正为 nvmap 的 ~218MB（与 jtop 工具一致），数据准确性大幅提升。

---

## 二、核心技术点（我学到了什么）

### 2.1 Jetson 统一内存架构（Unified Memory）

**知识点**：Jetson 的 GPU 和 CPU **共享同一块物理内存**（不像 PC 有独立显卡），所以：

- GPU 显存总量 = 系统可用内存总量（通过 `cudaMemGetInfo` 获取）
- GPU 显存使用量需要通过 NVIDIA 的 `nvmap` 驱动接口查询

### 2.2 GPU 频率获取：sysfs 接口

通过 Linux sysfs 文件系统读取 devfreq（动态频率调节）接口：

```cpp
// 当前频率（单位 Hz，需除以 100 万转 MHz）
/sys/class/devfreq/17000000.ga10b/cur_freq

// 最大频率
/sys/class/devfreq/17000000.ga10b/max_freq
```

> `17000000.ga10b` 是 Jetson Orin 的 GPU 设备节点（ga10b 是 NVIDIA Ampere 架构的 GPU 代号）。

### 2.3 GPU 显存获取：两种方法的对比（重点！）

| 方法                         | 数据源                                 | 原理                                | 准确性                                   |
| ---------------------------- | -------------------------------------- | ----------------------------------- | ---------------------------------------- |
| **CUDA API**           | `cudaMemGetInfo()`                   | 返回 CUDA 运行时管理的内存池信息    | ❌ 偏大（包含 CUDA context、驱动预留等） |
| **nvmap**（jtop 同款） | `/sys/kernel/debug/nvmap/iovmm/maps` | NVIDIA 驱动真实记录的各进程内存分配 | ✅ 准确（和 jtop 工具一致）              |

**实测对比**：

- CUDA API：~6900 MB（包含了 CUDA 内存池的预留）
- nvmap：~218 MB（真实被进程占用的 GPU 共享内存）

**nvmap 文件解析**（参考了开源工具 jtop 的实现）：

```
CLIENT      PROCESS       PID    BASE         SIZE(KB)
...
total                            223604K     ← 提取这一行
```

解析策略：

1. 优先解析 `total` 行（jtop 的做法）
2. 如果没有 total 行，累加所有进程行作为备用方案
3. 都失败则降级到 CUDA API（保证服务不中断）

### 2.4 降级容错设计（Fallback 机制）

```
nvmap 读取成功 → 返回 nvmap 数据（准确）
     ↓ 失败
CUDA API 获取 → 返回 CUDA 数据（偏大但有值）
     ↓ 失败
返回 -1（监控平台识别为数据无效）
```

**设计思想**：监控系统宁可给一个不太准的值，也不能没有值——这是高可用服务的常见设计。

### 2.5 Docker 容器的文件系统隔离问题（重点！）

**问题现象**：代码在宿主机上运行正常，但在 Docker 容器内 GPU 显存数据降级到了 CUDA API。

**排查过程**：

| 步骤 | 操作                                      | 发现                                                             |
| ---- | ----------------------------------------- | ---------------------------------------------------------------- |
| 1    | 对比 JSON 输出                            | `gpu_mem_usage` 和 `cuda_mem_usage` 数值完全一致 → 怀疑降级 |
| 2    | 容器内执行`ls /sys/kernel/debug/nvmap/` | 路径不存在                                                       |
| 3    | 容器内执行`mount \| grep debugfs`        | 无输出 → debugfs 未挂载                                         |
| 4    | 修改容器启动脚本添加挂载                  | 路径出现，但 sudo 报错                                           |
| 5    | 发现`sudo cat` 报错但 `cat` 能直接读  | sudo 插件权限损坏，且其实不需要 sudo                             |
| 6    | 代码改用普通`cat`                       | ✅ 问题解决                                                      |

**核心结论**：

1. **即使 `--privileged` 模式，Docker 也不会自动挂载 `/sys/kernel/debug`**，需要显式挂载：
   ```bash
   docker run -v /sys/kernel/debug:/sys/kernel/debug:ro ...
   ```
2. **容器中慎用 sudo**：容器内的 sudo 插件（sudoers.so）权限容易被破坏，而且挂载后普通 `cat` 就能读取
3. 宿主机需要先确保 debugfs 已挂载：`sudo mount -t debugfs none /sys/kernel/debug`

### 2.6 Linux 常用排查命令（实战积累）

```bash
# 查看文件权限（排查 sudo 权限问题的关键）
ls -la /usr/lib/sudo/sudoers.so      # 必须是 root:root
ls -la /etc/sudoers                  # 必须是 root:root，权限 440

# 检查 debugfs 挂载状态
mount | grep debugfs

# 验证 GPU 驱动
lsmod | grep nvgpu
```

---

## 三、修改的文件清单

| 文件                                                           | 修改内容                                                                                                                               |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `monitorservice3/src/device_probe/platform/jetson_probe.cpp` | 新增`GetGPUFreqMHz()`、`GetGPUFreqMAXMHz()`、`GetGPUMemoryUsedMB()`、`GetGPUMemoryTotalMB()`、`GetGPUMemoryUsage()` 五个函数 |
| `docker/scripts/docker_start.sh`                             | 在容器启动参数中添加`-v "/sys/kernel/debug:/sys/kernel/debug:ro"` 挂载                                                               |

---

## 四、简历包装（可以直接用）

### 版本 A：项目经历写法（推荐，适合应届生/1-3 年经验）

> **车路协同边缘计算平台 GPU 监控功能开发** | C++ / CUDA / Docker / Linux
>
> - 参与基于 NVIDIA Jetson 的车路协同边缘计算平台（AirOS）监控服务开发，负责设备状态采集模块中 **GPU 监控功能的扩展与容器化部署**
> - 基于 Linux sysfs/devfreq 接口实现 GPU 频率采集；**对比分析 CUDA API 与 NVIDIA nvmap 驱动接口两种显存采集方案**，采用 nvmap 方案（与 jtop 工具同源）将显存统计误差从 ~6.9GB 修正至真实的 ~218MB，数据准确性与开源工具 jtop 对齐
> - 设计三级降级容错机制（nvmap → CUDA API → 无效标记），保证监控服务在任何环境下不中断
> - **排查并解决 Docker 容器内 GPU 调试接口不可用的问题**：定位到容器默认不挂载 debugfs，通过修改容器启动脚本显式挂载 `/sys/kernel/debug`；同时移除对 sudo 的依赖，规避容器内 sudoers 权限配置缺陷
> - 最终监控数据以 JSON 格式实时上报云端，支撑运维平台对边缘设备的资源水位监控

### 版本 B：精简一句话（适合技能点罗列）

> 在 NVIDIA Jetson 平台开发 GPU 监控功能，通过 sysfs/devfreq 与 nvmap 驱动接口采集 GPU 频率和共享显存，解决 Docker 容器 debugfs 挂载与权限问题，实现容器化部署。

### 简历写作技巧

1. **用数字说话**：误差从 6.9GB → 218MB，这个对比非常有说服力
2. **突出排查过程**：面试官喜欢看你"如何发现问题 → 如何定位 → 如何解决"
3. **技术关键词**：CUDA、sysfs、debugfs、nvmap、Docker volume、降级容错——都是加分项
4. **不要夸大**：说"参与开发"而不是"主导设计"，面试经得起追问

---

## 五、面试高频问题与标准答案

### Q1：Jetson 的 GPU 显存和 PC 独立显卡有什么区别？

**答**：Jetson 采用**统一内存架构（Unified Memory）**，CPU 和 GPU 共享同一块物理内存，没有独立显存。所以 GPU 显存总量就是系统可用内存总量，需要用 CUDA 的 `cudaMemGetInfo` 获取总量，用 NVIDIA 的 nvmap 驱动接口查询真实占用。而 PC 的独立显卡有专用的显存（如 GDDR6），用 nvidia-smi 就能查。

### Q2：为什么不用 CUDA API 获取显存使用量？

**答**：`cudaMemGetInfo` 返回的是 **CUDA 运行时管理的内存池**信息，包含了 CUDA context、驱动预留内存、内存池碎片等，数值会明显偏大（我们实测 6.9GB vs 真实 218MB）。而 `/sys/kernel/debug/nvmap/iovmm/maps` 是 NVIDIA 驱动真实记录的各进程内存分配明细，和官方监控工具 jtop 是同一个数据源，更准确。所以我们优先用 nvmap，CUDA API 只作为降级方案和获取总量的手段。

### Q3：你的代码怎么做容错的？

**答**：设计了三级降级机制：

1. 优先读取 nvmap 文件，解析 `total` 行
2. 如果 nvmap 读不到（路径不存在、权限问题），降级到 `cudaMemGetInfo`，虽然偏大但保证有值
3. 如果 CUDA 也失败，返回 -1，监控平台识别为无效数据

监控系统的设计原则是**宁可数据不精确，也不能服务中断**。

### Q4：Docker 容器里为什么访问不到 /sys/kernel/debug？

**答**：Docker 容器有独立的文件系统视图，`/sys/kernel/debug`（debugfs）属于内核调试接口，**默认不会挂载到容器中，即使加了 `--privileged` 也不会**。需要在 `docker run` 时显式挂载：`-v /sys/kernel/debug:/sys/kernel/debug:ro`。另外宿主机要先确保 debugfs 已挂载（`mount -t debugfs none /sys/kernel/debug`）。

### Q5：为什么最后去掉了 sudo？

**答**：两个原因：

1. **sudo 在容器里不可靠**：容器内的 `/usr/lib/sudo/sudoers.so` 权限容易被破坏（我们遇到了 "must be owned by uid 0" 的错误），而且容器镜像里未必配置了免密 sudo
2. **其实不需要 sudo**：挂载 debugfs 之后，容器内的用户已经有读取权限了，直接 `cat` 就能读到数据

去掉 sudo 还避免了 `popen("sudo cat ...")` 因权限失败后静默降级的问题——这个 bug 很隐蔽，因为错误被 `2>/dev/null` 吞掉了。

### Q6：你是怎么定位到"代码降级到 CUDA API"这个 bug 的？

**答**：通过**数据比对**。我发现 JSON 输出里 `system.cuda_mem_usage` 和 `system.gpu_mem_usage(%)` 两个字段数值完全一致（都是 29.78%），但这两个指标的数据源不同，理论上不应该相等。由此怀疑 GPU 显存函数实际走了 CUDA 分支。然后在代码里加调试日志，打印 `popen` 的返回状态（exit_status=256 表示命令执行失败），最终定位到 sudo 权限问题。

> 💡 这个回答展示了"从数据异常反推代码路径"的调试思维，面试官会很认可。

### Q7：sysfs 是什么？和 procfs 有什么区别？

**答**：两者都是 Linux 的伪文件系统（内存中的虚拟文件系统）：

- **procfs**（`/proc`）：主要反映**进程**信息（`/proc/[pid]/`）和部分内核状态（`/proc/meminfo`、`/proc/stat`）
- **sysfs**（`/sys`）：主要反映**设备和驱动**信息，按设备模型组织（`/sys/class/`、`/sys/devices/`）

我获取 GPU 频率就是读 `/sys/class/devfreq/` 下 GPU 设备节点的 `cur_freq`，这是内核 devfreq（动态频率调节）框架暴露的标准接口。

### Q8：popen 和直接用 fopen 读文件有什么区别？为什么显存用 popen？

**答**：`fopen` 直接打开文件，`popen` 是启动一个 shell 子进程执行命令并读取其输出。

- GPU 频率文件用 `ifstream` 直接读就够了（普通 sysfs 文件）
- nvmap 文件理论上也可以用 `ifstream` 读，但用 `popen` 执行 `cat` 的好处是：如果文件权限有问题，命令会失败返回非零退出码，方便做降级判断；而且以后如果需要 sudo 提权，只需要改命令字符串

### Q9：如果让你继续优化这个监控功能，你会做什么？

**答**（加分回答）：

1. **性能优化**：目前每次都启动 `cat` 子进程，开销较大，可以改成直接 `ifstream` 读取并自己处理权限异常
2. **可配置性**：nvmap 路径在不同 Jetson 型号上可能不同（比如 Xavier 和 Orin），可以做路径自动探测
3. **GPU 利用率**：目前 `/sys/devices/gpu.0/load` 在某些型号上不存在，可以结合 tegrastats 或 NVML 获取
4. **告警阈值**：在采集层增加显存/温度的阈值告警，减少云端压力

### Q10：Docker 容器和虚拟机的区别是什么？（延伸题）

**答**：虚拟机通过 Hypervisor 模拟完整硬件，每个 VM 有独立内核；Docker 容器通过 Linux 内核的 **namespace**（隔离视图）和 **cgroups**（限制资源）实现进程级隔离，共享宿主机内核。所以容器更轻量，但也意味着像 `/sys/kernel/debug` 这种内核接口默认是不可见的，需要显式挂载。

---

## 六、踩坑记录（血泪教训）

| 坑                        | 现象                                  | 解决方案                                          |
| ------------------------- | ------------------------------------- | ------------------------------------------------- |
| `--privileged` 不是万能 | 容器内仍访问不到 debugfs              | 显式`-v /sys/kernel/debug:/sys/kernel/debug:ro` |
| sudo 插件权限损坏         | `sudoers.so must be owned by uid 0` | `chown root:root` 修复，或干脆不用 sudo         |
| `/etc/sudoers` 属主错误 | `owned by uid 1000, should be 0`    | `chown root:root /etc/sudoers && chmod 440`     |
| `2>/dev/null` 吞错误    | 看不到 sudo 失败的真正原因            | 调试时先去掉错误重定向                            |
| 容器内无 systemd          | `Failed to create bus connection`   | 正常现象，忽略即可                                |

---

## 七、一句话总结

> 我通过阅读源码、数据比对定位问题、逐层排查（文件系统挂载 → 命令权限 → 代码逻辑），最终在 Docker 容器中实现了准确的 Jetson GPU 频率与共享显存监控，并沉淀了一套容器化部署的排查方法论。
