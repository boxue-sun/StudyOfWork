一、上次回顾：

1完成从 IDM 切换到带 1.5s 时延的 ACC 法规模型；

完成 200 组参数变体实验，得到单跟车场景安全边界，可以复现「前车中途急刹追尾碰撞」典型危险场景。


2纠正概念：我原先的方案不属于强化学习，本质是监督学习。

3新方向建议：建议我转向**代理模型 + 贝叶斯优化**的路线，用来解决网格穷举带来的状态爆炸问题。小样本搜索高风险碰撞参数。

4任务：调研危险场景搜索 SOTA 文献，找到现有方法优缺点，。


二、最近工作：

	1.GA2AD论文：

	

- GA2AD 做什么：对抗强化学习，在 SUMO 仿真环境生成高风险危险场景
- **可借鉴 3 点**：
  1. 风险评估不能只看 “碰撞 / 安全”0‑1 标签，可以用连续风险分数；
  2. 需要控制障碍物对抗强度，过滤现实中不合理的极端行为；
  3. 不要无脑全部穷举，采用智能搜索，优先挖掘高风险区域。
- **不能照搬整套框架的原因**：
  GA2AD 目标是一边生成危险场景、一边训练提升主车；但我的主车是**固定不变的 ACC 法规模型，不需要训练更新**；并且它是纯仿真采样，没有形式化完备证明，如果直接照搬就丢掉 UPPAAL 模型检验的核心优势。


### 3 贝叶斯优化方法思路（3‑4 分钟）

1. 痛点：目前网格穷举，大量参数都是安全无效样本，每一次调用 UPPAAL 开销大，参数一多就出现组合爆炸。
2. 通俗讲贝叶斯优化：两大组件「代理模型」+「采集函数」，平衡**开发（深挖已知高风险区域）、探索（探索未知参数空间）**，不用均匀遍历所有参数，优先挑选最有可能发生碰撞的候选参数。
3. **我的结合方案（重点）**
   - 搜索参数：两车初始距离、自车初速度、障碍物初速度 3 个参数；
   - ⚠️**UPPAAL 作为真值评估器**：贝叶斯优化只负责搜索候选参数，代理模型只是猜测，猜测结果不能当结论，候选参数必须送入 UPPAAL 做形式化校验，完备性来自 UPPAAL。
4. Demo 演示：目前我写了模拟 demo，用模拟函数 mock 代替 UPPAAL 验证整套搜索逻辑；还没有完成和真实 UPPAAL 的接口对接。



结合我现在 UPPAAL 的跟车法规模型，贝叶斯优化整套工作流是这样的。


我们的待搜索参数一共三维：初始距离 gap、自车初速度、障碍物初速度，优化目标最大化碰撞风险。
首先生成少量初始样本，每一组都交给 UPPAAL 拿到真实风险，构建初始数据集。
然后代理模型高斯过程，根据已有的样本，对整个参数空间做风险预测，同时输出每个位置的不确定性。
再通过 EI 期望改进采集函数，平衡开发和探索，选出下一轮最有潜力出现碰撞的候选参数。
**重点，候选参数不能直接信代理模型的预测，必须送入 UPPAAL 做形式化真值校验，得到是否碰撞可达和风险值。**
把新样本加入数据集，循环迭代，直到用完我们设置的 UPPAAL 调用预算。
和现有很多贝叶斯优化自动驾驶论文不一样，别人评估器是 CARLA 仿真，我这里评估器是 UPPAAL，可以数学证明碰撞路径是否可达。
贝叶斯优化只负责提升搜索效率，完备性完全来自 UPPAAL。





1. 真实实验（调用 UPPAAL）：得到少量真实数据：`[距离，自车速度，障碍车速度] →真实风险`
2. 把这些真实数据喂给**高斯过程（代理模型）**
3. GP 脑补整个三维参数空间：对任意一组 (gap,v_ego,v_obs) 输出两个结果：
   - 预测风险（均值）
   - 不确定性（方差）
4. 采集函数 EI 结合上面两个值，选出下一个最值得跑 UPPAAL 的候选参数
5. **候选参数不能信 GP 预测！送入 UPPAAL 做真实校验，拿到真正风险。**
6. 把新结果加入数据集，重新训练高斯过程，循环。


| 类别                          | 代表文献                                                | 核心优势                                            | 共同局限性                                                    |
| ----------------------------- | ------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------- |
| 1. 形式化建模验证 AEB         | 基于扩展着色混合 Petri 网的 AEB 验证                    | 可完成系统形式化属性分析，可验证可达性              | 测试场景**人工给定** ，缺少自动高危参数搜索，易状态爆炸 |
| 2. 搜索式对抗测试（仿真后端） | TuRS‑BSE (贝叶斯优化)、PAFOT (遗传算法)、GA2AD 对抗 RL | 智能搜索高危场景，搜索效率高；具备成熟风险评价指标  | **评估器为仿真；仅采样观测碰撞，无形式化可达证明**      |
| 3. 数据 / 工业安全驱动        | SOTIF 可预见误用 AEB 研究、因果贝叶斯网络场景生成       | 对接工业 SOTIF 标准；结合真实事故数据，场景贴近现实 | 依赖仿真评估；不擅长定位参数临界安全边界                      |


**研究缺口（投屏也要显示）**

> 现有工作很少将「贝叶斯优化智能参数搜索」与「形式化模型检验真值评估」相结合。
> 本工作思路：外层贝叶斯优化做搜索；内层 UPPAAL 作为真值评判，兼顾搜索效率与碰撞可达完备证明。
>



目前我完成了 mock 模拟 demo 验证逻辑，下一步开发接口对接真实 UPPAAL。
同时这里我也有疑问：风险指标我应该用 0‑1 碰撞二值，还是 TTC 转换的连续风险值更适合做优化？”




## PPT‑13 文献：形式化建模类（Petri 网 AEB 验证）

标题：文献 1｜基于扩展着色混合 Petri 网的 AEB 系统建模与功能验证

- **核心工作**
  使用 ECHPN 混合 Petri 网对 AEB 系统建模；支持传感器 / 执行器故障检测定位；模型可转换到 Simulink 仿真。

✅优点

1. 同时刻画离散控制逻辑 + 车辆连续动力学
2. 支持硬件故障建模与溯源
3. 打通形式模型向工业仿真工具转换

❌局限性

1. 需要人工给定测试场景，**无自动搜索高危参数能力**
2. 模型规模增大同样面临状态爆炸

💡对本工作启示

> 纯形式化验证缺少自动搜索手段，可引入贝叶斯优化弥补搜索效率短板。

> ⚠️投屏文字到此为止，细节你嘴巴讲。老师追问，你再口头展开。

---

## PPT‑14 文献：搜索‑基于仿真类（两篇放同一页投屏）

标题：文献 2、3｜搜索式对抗测试（评估器均为仿真）

### 文献 2 TuRS‑BSE（贝叶斯优化自动驾驶安全评估）

- 核心：高斯过程代理模型 + 信任域搜索，定位系统安全边界，小样本加速测试
  ✅优点：针对安全边界优化，减少评估调用次数
  ❌局限：后端为仿真，**无可达性完备证明；高维参数 GP 性能退化**
  💡启示：为本工作提供基线思路；区别：本工作评估器替换为 UPPAAL 形式化检验

### 文献 3 PAFOT 遗传算法对抗场景生成

- 核心：九宫格位置编码 + 遗传算法生成对抗危险场景，METTC 等风险指标
  ✅优点：完整对抗搜索范式，风险评价指标可复用；提供实验对比思路
  ❌局限：CARLA 仿真作为评估器，无法证明场景可达，计算开销大
  💡启示：METTC/TTC 风险指标，可用于构建贝叶斯优化目标函数

---

## PPT‑15 文献：工业安全 & 数据驱动类（两篇放一页投屏）

标题：文献 4、5｜SOTIF 安全标准 & 因果贝叶斯网络场景生成

### 文献 4 SAE：AEB 可预见误用 SOTIF 验证

- 核心：基于 ISO21448 SOTIF，STPA 风险分析 + 仿真 + 驾驶员在环实验，评估系统能力边界带来的风险
  ✅优点：对齐工业预期功能安全标准
  ❌局限：依赖仿真，不自动生成临界危险场景
  💡启示：UPPAAL 求出的安全边界，对应 SOTIF 的系统能力边界，提升本工作工程价值

### 文献 5 基于因果贝叶斯网络的风险场景生成

- 核心：基于真实事故数据集构建因果网络，推理生成高风险测试场景，CARLA 仿真评估
  ✅优点：结合真实事故数据，因果推理生成场景更贴合现实
  ❌局限：仿真做评估；不擅长精细定位参数临界安全边界；依赖数据集质量
  💡启示：多维度风险指标可借鉴；因果网络放在**未来拓展工作**，现阶段不作为重点。


# 《Risk Scenario Generation for Autonomous Driving Systems based on Causal Bayesian Networks》

（中文：基于因果贝叶斯网络的自动驾驶风险场景生成，arXiv 2405.16063）

## 一、研究背景

自动驾驶虚拟测试很重要，但**纯随机生成场景效率很低**，大量生成安全无效场景；
传统贝叶斯网络只能学到变量之间**相关性**，分不清谁是因、谁是果；
这篇用**因果贝叶斯网络 CBN**，基于真实事故数据，挖掘天气、路面、光照、车辆行为之间的**因果关系**，定向推理生成高风险测试场景，再送入 CARLA 仿真执行评估arXiv。

> 普通贝叶斯网络 BN：只能看到 “A 和 B 一起发生”（相关性）；
> 因果贝叶斯网络 CBN：能回答 “主动改变 A，会不会造成 B 发生”（干预、因果推理），适合做场景干预生成危险工况。

## 二、整套完整工作流水线（4 大步）

1. **CBN 因果网络构建**
   使用马里兰州公开真实事故数据集，做数据预处理；结合专家先验知识，用因果发现算法学习因果图（有向无环图 DAG）；做模型验证（SHD、SID 指标、反驳测试），得到最终因果贝叶斯网络。节点包含：天气、路面条件、光照、车辆行为等事故关键因素arXiv。
2. **风险场景推理生成算法**
   输入少量**种子功能场景**（比如跟车、车道保持、超车）；
   CBN 网络推理，挖掘在这个种子场景下，哪些环境 + 行为组合会提升事故风险；
   过滤不合理条件，输出一批高风险逻辑场景。
3. **场景格式转换**
   把抽象逻辑风险场景 → 转换成 CARLA 仿真可以运行的可执行具体场景。
4. **仿真评估打分**
   CARLA 运行生成的场景；从**安全性、舒适性、效率**三个维度打分；安全性指标：TTC 碰撞时间、最小距离、碰撞是否发生等；最后输出筛选后的高风险场景集合arXiv。

### 实验结果

输入 5 个种子场景（跟车、车道保持、超车等），总共生成**89 个高风险场景**。
对比基线：随机采样、DriveFuzz 模糊测试；

- 生成单个有效风险场景需要的迭代次数更少；
- 仿真器启动次数更少，总耗时更低，效率优于对比方法arXiv。

## 三、论文优点

1. **使用真实交通事故数据集**，生成场景不是凭空瞎编，贴近现实世界事故模式，对 SOTIF 预期功能安全有参考价值。
2. 用**因果推理而不只是相关性统计**，可以主动干预变量去制造风险，而不是被动统计已有数据。
3. 整套端到端流水线：因果网络推理 → 场景转化 → CARLA 仿真评估；多维度评价指标（安全、舒适、效率）可以直接拿来借鉴。
4. 离线训练一次 CBN 模型，后续可以反复复用生成不同种子场景的风险案例。

## 四、论文存在的短板（写 related‑work 重点用）

1. **真值评估完全依赖 CARLA 仿真采样**。仿真跑出来碰撞，只代表这一次仿真轨迹出现碰撞；**不能数学证明该场景下碰撞路径一定可达，没有形式化完备可达性保证**。有可能漏掉潜在危险工况。
2. **因果网络质量高度依赖事故数据集质量**，数据集偏差会直接导致生成的风险场景出现偏差。
3. 它生成的是**环境 + 行为组合的场景**，不是专门搜索「连续参数空间里面的临界碰撞边界」，不擅长精细定位安全边界。
4. 没有处理混合离散‑连续时间自动机这类混合动力学系统，和 UPPAAL 这类形式化工具完全没有结合。

## 五、对你课题的启发（汇报直接复制）

1. **短期参考**：
   它的多维度风险评估指标（TTC、最小距离、舒适性指标），我在贝叶斯优化的**奖励 / 目标函数设计可以借鉴**，不只使用简单的碰撞 0‑1 二值标签。
2. **中长期拓展思路（可以写进论文未来工作）**：
   我现在 UPPAAL + 贝叶斯优化只做纯参数搜索；后续可以借鉴因果贝叶斯网络思路，**引入真实事故的因果 / 概率先验知识**，区分 “理论上危险，但现实极少发生” 和 “现实高频危险” 的参数组合，提升生成场景的工程实用性。
3. **明确区分创新差异**：
   该论文是【因果网络生成场景 + 仿真做评估】；我的工作是【贝叶斯优化搜索参数 + UPPAAL 形式化模型检验作为真值评估器】，我拥有形式化可达性完备保证，这是该论文缺失的点。
